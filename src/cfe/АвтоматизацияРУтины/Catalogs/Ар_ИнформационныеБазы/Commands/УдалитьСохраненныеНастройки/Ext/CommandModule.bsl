
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Форма		= ПараметрыВыполненияКоманды.Источник;
	Ключ 		= "СохраненныеНастройки";
	
	Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма, Ключ)
		И ТипЗнч(Форма[Ключ]) = Тип("Структура") Тогда
		
		Форма[Ключ] = Новый Структура("СоответствияПараметровКластера, СоответствиеПараметровИнформационныхБаз", Новый Соответствие, Новый Соответствие);
		
	КонецЕсли;
	
КонецПроцедуры
